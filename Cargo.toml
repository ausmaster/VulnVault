[package]
name = "rustyVault"
version = "0.1.0"
edition = "2021"

[lib]
name = "rustyVault"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rayon = "1.10"
strsim = "0.11"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1